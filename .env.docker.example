# Project-wide Docker env example
# Copy to `.env` at repo root when using docker-compose.

# Frontend external port mapping (host:container  -> ${FRONTEND_HTTP_PORT}:80)
FRONTEND_HTTP_PORT=5173

# Vite dev server allowed hosts (used when running Vite dev)
# Special: "*" or "true" allows all hosts (not recommended).
ALLOWED_HOSTS=sampele.net


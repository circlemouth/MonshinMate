# MonshinMate environment variables sample
#
# このファイルをリポジトリルートの `.env` にコピーすると、Docker Compose が同じ値を参照します。
# バックエンドを単体起動する場合も、同じ `.env` を `backend/.env` にコピーして利用できます。

# ===== ホスト公開ポート =====
FRONTEND_HTTP_PORT=5173

# ===== SQLite 永続化 =====
# Docker Compose では `./data/sqlite` をコンテナ内 `/app/data/sqlite` にマウントします。
# 既存の `backend/app/app.sqlite3` を流用する場合は、この値を旧パスに変更するか、
# `./data/sqlite/app.sqlite3` にファイルを移動してください。
MONSHINMATE_DB=/app/data/sqlite/app.sqlite3

# ===== CouchDB 接続設定 =====
COUCHDB_URL=http://couchdb:5984/
COUCHDB_DB=monshin_sessions
COUCHDB_USER=admin
COUCHDB_PASSWORD=admin

# ===== 管理者設定（必要に応じて変更） =====
#ADMIN_PASSWORD=change-this-on-first-boot
# 非常用パスワードリセット用の共有シークレット
ADMIN_EMERGENCY_RESET_PASSWORD=strong-unique-secret-here
# JWT 署名キー。実運用では十分に長いランダム値に置き換えてください。
#SECRET_KEY=a_very_secret_key_that_should_be_changed
# TOTP シークレット暗号化用キー（URL-safe base64 32bytes）。
#TOTP_ENC_KEY=
# Vite dev server の許可ホスト。`*` または `true` で全許可（推奨しません）。
#ALLOWED_HOSTS=localhost

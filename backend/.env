# MonshinMate backend environment example
#
# 本ファイルを `.env` としてコピーし、必要な値を設定してください。
#
# 初期管理者パスワード（既定 'admin'）。初回セットアップ時のみ判定に使用されます。
#ADMIN_PASSWORD=change-this-on-first-boot

# 非常用リセット用パスワード（TOTP 無効時のみ有効）。
# 管理画面の「パスワードリセット」で `ADMIN_EMERGENCY_RESET_PASSWORD` と一致する値を
# 入力すると、管理者パスワードをリセットできます。十分に強度の高い値を設定し、
# サーバ上でのみ管理してください。
ADMIN_EMERGENCY_RESET_PASSWORD=strong-unique-secret-here


